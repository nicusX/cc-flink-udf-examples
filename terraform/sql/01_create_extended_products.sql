CREATE TABLE `extended_products` (
  `product_id`      STRING NOT NULL,
  `long_name`       STRING,
  `extended_name`   STRING,
  `extra_long_name` STRING,
  PRIMARY KEY (`product_id`) NOT ENFORCED
);